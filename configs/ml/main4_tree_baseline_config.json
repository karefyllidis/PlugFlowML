{
  "_comment": "Main_4_train_and_evaluate_tree_models_IO.ipynb — exit-plane tree baselines; optional BayesSearchCV tuning (off by default). Keys consumed: top-level test_size/random_state/models_to_train; random_forest, xgboost, gradient_boosting, adaboost blocks; tuning.*. See docs/ML_CONFIG_GUIDE.md.",
  "test_size": 0.2,
  "random_state": 42,
  "models_to_train": ["random_forest", "gradient_boosting", "xgboost", "adaboost"],
  "random_forest": {
    "n_estimators": 100,
    "max_depth": 20
  },
  "xgboost": {
    "n_estimators": 100,
    "max_depth": 6,
    "learning_rate": 0.3,
    "subsample": 1.0,
    "colsample_bytree": 1.0,
    "reg_alpha": 0.0,
    "reg_lambda": 1.0
  },
  "gradient_boosting": {
    "n_estimators": 150,
    "max_depth": 5
  },
  "adaboost": {
    "n_estimators": 200,
    "learning_rate": 0.1,
    "max_depth": 6
  },
  "tuning": {
    "_comment": "BayesSearchCV budget for Section 7, only used when IF_HYPERPARAM_TUNING=True (off by default).",
    "n_iter": 60,
    "patience": 10,
    "min_delta": 0.0005,
    "cv": 3,
    "scoring": "neg_mean_absolute_error"
  }
}
