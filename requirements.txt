# HydrAI — Project dependencies
# ==============================
# Run: pip install -r requirements.txt

# ── Chemical kinetics ─────────────────────────────────────────────────────────
cantera>=3.1.0

# ── Numerical & data ──────────────────────────────────────────────────────────
numpy>=1.24.0,<3
scipy>=1.9.0
pandas>=2.2.0,<3
pyarrow>=14.0.0

# ── Visualization ─────────────────────────────────────────────────────────────
matplotlib>=3.7.0

# ── Machine learning ───────────────────────────────────────────────────────────
scikit-learn>=1.3.0
scikit-optimize>=0.10.2
xgboost>=2.0.0
joblib>=1.3.0

# ── Deep learning (PyTorch — notebooks/Main_7_train_evaluate_SimpleNN_full_profile.ipynb) ──
# CPU build works out of the box; for CUDA / ROCm follow the official selector
torch>=2.0.0

# ── Notebooks & pipeline (run_pipeline.py / nbconvert) ───────────────────────
jupyter>=1.0.0
ipykernel>=6.25.0
nbconvert>=7.10.0

# ── Optional (uncomment as needed) ────────────────────────────────────────────
# Hyperparameter search for Main_7 Section 6b (IF_HYPERPARAM_TUNING=True).
optuna>=3.4.0
# Textual layer-by-layer model summary for Main_7 Section 6c (IF_ARCH_SUMMARY=True).
torchinfo>=1.8.0
lightgbm>=3.3.0

# ── Development & testing ─────────────────────────────────────────────────────
pytest>=7.4.0
